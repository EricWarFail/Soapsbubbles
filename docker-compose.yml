services:
  seifenblasen:
    build: .
    container_name: seifenblasen
    restart: unless-stopped
    networks:
      - caddy_net   # must match the network your Caddy container is on

networks:
  caddy_net:
    external: true
